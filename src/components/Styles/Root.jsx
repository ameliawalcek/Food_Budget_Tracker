import { makeStyles } from '@material-ui/core'

export const useRootStyles = makeStyles((theme) => ({
    paperRoot: {
        height: '92vh',
        paddingTop: '8vh',
    },
    paperRootTwo:{
        height: '100%'
    }
}))